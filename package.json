{
  "name": "jobpilot",
  "version": "0.0.0",
  "private": true,
  "description": "Template for building agentic apps on the ggui protocol — Claude Agent SDK variant.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@11.2.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:ggui": "dotenv -e .env.local -- pnpm --filter ./servers/ggui start",
    "dev:mcps": "pnpm --parallel --filter \"./servers/mcps/*\" run dev",
    "dev:todo": "pnpm --filter ./servers/mcps/todo dev",
    "dev:agent": "dotenv -e .env.local -- pnpm --filter ./servers/agent dev",
    "dev:web": "pnpm --filter ./apps/web dev",
    "dev:stop": "node scripts/stop-dev.mjs",
    "build": "pnpm --recursive run build",
    "typecheck": "pnpm --recursive run typecheck",
    "deploy:railway": "dotenv -e .env.local -- node scripts/deploy-railway.mjs",
    "demo-smoke": "DEMO_FIXTURE=1 playwright test e2e/demo-smoke.spec.ts",
    "demo-smoke:live": "DEMO_LIVE=1 playwright test e2e/demo-smoke-live.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "dotenv-cli": "^8.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}