{
  "name": "@jobpilot/mcp-commute",
  "version": "0.0.1",
  "description": "Commute MCP server: multimodal commute comparison + weather + smart departure planning (US-003).",
  "private": true,
  "type": "module",
  "bin": {
    "jobpilot-mcp-commute": "./dist/index.js"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}
